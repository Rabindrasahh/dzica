export default {
    transactions(state) {
        return state.transactions;
    },
    singlePost(state) {
        return state.post;
    },
    followingPosts(state) {
        return state.followingPosts;
    },
};
