export default {
    userPosts(state) {
        return state.userPosts;
    },
    singlePost(state) {
        return state.post;
    },
    followingPosts(state) {
        return state.followingPosts;
    },
};
